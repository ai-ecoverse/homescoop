# Homebrew-shaped recipe — declarative metadata; body is build.sh (host) or build.jsh (slicc).
name: libwebp
builder: slicc
# renovate: datasource=git-tags depName=webmproject/libwebp extractVersion=^v(?<version>.*)$
version: "1.5.0"
npm: "@ai-ecoverse/wasm-libwebp"
summary: "WebP codec libraries"
source:
  # Filled when packaging real ladder artifacts; stubs reserve the npm name only.
  url: null
  sha256: null
dependencies:
  build: []
  host: []
about:
  homepage: https://github.com/ai-ecoverse/homescoop/tree/main/packages/libwebp
  license: SEE_UPSTREAM
