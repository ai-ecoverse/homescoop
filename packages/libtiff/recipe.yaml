# Homebrew-shaped recipe — declarative metadata; body is build.sh (host) or build.jsh (slicc).
name: libtiff
builder: slicc
# renovate: datasource=git-tags depName=libsdl-org/libtiff extractVersion=^v(?<version>.*)$
version: "4.7.0"
npm: "@ai-ecoverse/wasm-libtiff"
summary: "TIFF library"
source:
  # Filled when packaging real ladder artifacts; stubs reserve the npm name only.
  url: null
  sha256: null
dependencies:
  build: []
  host: []
about:
  homepage: https://github.com/ai-ecoverse/homescoop/tree/main/packages/libtiff
  license: SEE_UPSTREAM
