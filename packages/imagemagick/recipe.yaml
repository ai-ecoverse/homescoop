# Homebrew-shaped recipe — declarative metadata; body is build.sh (host) or build.jsh (slicc).
name: imagemagick
builder: slicc
# renovate: datasource=github-releases depName=ImageMagick/ImageMagick versioning=loose
version: "7.1.2-31"
npm: "@ai-ecoverse/wasm-imagemagick"
summary: "ImageMagick (slicc ladder utilities/magick)"
source:
  # Filled when packaging real ladder artifacts; stubs reserve the npm name only.
  url: null
  sha256: null
dependencies:
  build:
    - zlib
    - libjpeg-turbo
    - libpng
    - lcms2
    - libtiff
    - libwebp
    - openjpeg
    - freetype
    - libxml2
  host: []
about:
  homepage: https://github.com/ai-ecoverse/homescoop/tree/main/packages/imagemagick
  license: SEE_UPSTREAM
