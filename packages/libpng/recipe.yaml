# Homebrew-shaped recipe — declarative metadata; body is build.sh (host) or build.jsh (slicc).
name: libpng
builder: slicc
# renovate: datasource=git-tags depName=pnggroup/libpng extractVersion=^v(?<version>.*)$
version: "1.6.50"
npm: "@ai-ecoverse/wasm-libpng"
summary: "libpng PNG library"
source:
  # Filled when packaging real ladder artifacts; stubs reserve the npm name only.
  url: null
  sha256: null
dependencies:
  build:
    - zlib
  host: []
about:
  homepage: https://github.com/ai-ecoverse/homescoop/tree/main/packages/libpng
  license: SEE_UPSTREAM
