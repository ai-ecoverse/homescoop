# Homebrew-shaped recipe — declarative metadata; body is build.sh (host) or build.jsh (slicc).
name: freetype
builder: slicc
# Upstream tags are VER-MAJOR-MINOR-PATCH (freetype/freetype2); Renovate mapping TBD.
version: "2.13.3"
npm: "@ai-ecoverse/wasm-freetype"
summary: "FreeType font engine"
source:
  # Filled when packaging real ladder artifacts; stubs reserve the npm name only.
  url: null
  sha256: null
dependencies:
  build: []
  host: []
about:
  homepage: https://github.com/ai-ecoverse/homescoop/tree/main/packages/freetype
  license: SEE_UPSTREAM
